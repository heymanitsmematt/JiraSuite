using System;
using System.Xml;

namespace JiraSuite.Managers
{
    public static class UpdateManager
    {
        private static string _updateFilePath = $"{System.Environment.CurrentDirectory}/updateTracker.xml";
        private static DateTime _updateTime;
        private static XmlDocument _updateDocument;

        private static void Update()
        {
            
        } 

        
    }
}